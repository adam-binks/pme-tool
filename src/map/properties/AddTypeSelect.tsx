import { useContext } from "react";
import { MapContext } from "../Map";

interface AddTypeSelectProps {

}
export function AddTypeSelect({ }: AddTypeSelectProps) {
    const mapId = useContext(MapContext)
}